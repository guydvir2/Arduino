#include <ctype.h>
char *a="GUY_DVIR";
char parameters [3][5];
void setup() {
  // put your setup code here, to run once:
  int b = strlen(a);
  char c[20];
  Serial.begin(9600);
  Serial.println(isdigit('1'));

  
  
  
//  Serial.println(strlen(parameters));
 
}

void loop() {
  // put your main code here, to run repeatedly:

}
